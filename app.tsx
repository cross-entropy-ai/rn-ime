import { ScrollView, Text } from "react-native";

export default function App() {
  return (
    <ScrollView
      style={{
        padding: 20,
      }}
    >
      <Text>Hello World!</Text>
    </ScrollView>
  );
}
